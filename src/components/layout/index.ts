/**
 * Layout Components — Barrel Export
 *
 * Responsive layout shell and grid utilities for the Portal.
 * All layouts prevent horizontal scrolling across 320–2560px and
 * collapse navigation at ≤768px (Requirements 8.1, 8.2, 8.3).
 */

export { ResponsiveLayout } from './ResponsiveLayout';
export type { ResponsiveLayoutProps } from './ResponsiveLayout';

export { ResponsiveGrid, ResponsiveGridItem } from './ResponsiveGrid';

export { default as UnifiedDashboardLayout } from './UnifiedDashboardLayout';
