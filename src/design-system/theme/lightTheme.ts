/**
 * Light Theme — Gurukul AI
 *
 * The default theme. Warm, educational aesthetic with deep blue primary
 * and amber/gold secondary tones.
 */

import { createGurkulTheme } from './createTheme';

export const lightTheme = createGurkulTheme({ mode: 'light' });
