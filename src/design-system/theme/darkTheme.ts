/**
 * Dark Theme — Gurukul AI
 *
 * Dark mode variant preserving the educational identity with adjusted
 * luminance levels for comfortable low-light usage.
 */

import { createGurkulTheme } from './createTheme';

export const darkTheme = createGurkulTheme({ mode: 'dark' });
